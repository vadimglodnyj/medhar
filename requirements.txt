# Мінімальні залежності для python app.py (медична характеристика). Python 3.8+.
setuptools>=65.0.0
wheel>=0.40.0
numpy>=1.24.0
Flask>=2.3.0
Werkzeug>=2.3.0
pandas>=2.0.0
python-docx>=0.8.11
docxtpl>=0.16.7
openpyxl>=3.1.0
pymorphy3>=2.0.0
pymorphy3-dicts-uk>=2.4.0
python-dotenv>=1.0.0
google-genai>=1.0.0
dropbox>=12.0.0
# Pure-Python клієнт спільної Turso/libSQL БД (працює на Windows/Python 3.14)
libsql-client>=0.3.1
# Шифрування спільного пакету команди (PIN)
cryptography>=42.0.0
# Desktop-вікно (Edge WebView2)
pywebview>=5.0.0
# PDF-направлення (конвертація DOCX→PDF через Microsoft Word на Windows)
pywin32>=306; platform_system=="Windows"

# Збірка Windows Install.exe (лише для розробника):
#   pip install pyinstaller
#   .\desktop\build.ps1
